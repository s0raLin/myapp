name: myapp
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.2.0
  provider: ^6.1.5+1
  shared_preferences: ^2.5.5
  image_picker: ^1.1.2
  file_picker: ^11.0.2
  mime: ^2.0.0
  audiotags: ^1.4.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/
    - lib/assets/images/
